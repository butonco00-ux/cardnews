# 부동산 인스타 카드뉴스 자동 생성·게시 프로그램 — 빌드 프롬프트 (v1, 2026-09-17)

> **2026-09-18 갱신**: 맨 아래 **v2 — 실제 구축 기록**(바뀐 결정·오류와 해결·다음 계획)이 우선입니다.
> **사용법**: 이 파일 전체를 Claude Code(또는 비슷한 코딩 AI)에 붙여 넣으세요. 빈 폴더에서 시작합니다.
> **매일 아침 GitHub이 자동으로** 부동산 정책·세금 보도자료와 뉴스 헤드라인을 모아 인스타 카드뉴스(4:5, 최대 10장)와 캡션을 만듭니다. **아이패드·휴대폰으로 확인 페이지를 열어** 보고 [인스타에 올리기]를 누르면 게시됩니다.
> 비용 0원(GitHub 공개 저장소 + Actions + Pages, 인스타 공식 API, 네이버 검색 API, 정책브리핑).
> 형식은 `제안서자동화.md`(v3)를 따랐고, 그때 겪은 문제(추측 호출, 한글 requirements, 테스트가 사용자 데이터 덮어쓰기, 로그인 만료)를 미리 반영했습니다.

---

## 0. 지시 (이 프롬프트를 받은 AI에게)

아래 명세대로 **처음부터 끝까지 구현하고, 스스로 검증한 뒤** 보고하라.

- **의뢰인은 비개발자 공인중개사다.** 에러 원문·코드를 보여주지 말고 원인을 고쳐라. 안내·보고는 쉬운 한국어, 한 번에 하나씩.
- **추측하지 말고 먼저 열어서 확인하라.** 정책브리핑 주소·페이지 구조, 네이버 API, Instagram API 규격·한도는 **지금 실제 문서와 페이지를 열어 본 뒤** 코드를 써라. (제안서 때 공공 API 주소를 추측해 호출했다가 `NO_OPENAPI_SERVICE`로 실패한 적이 있다.)
- **"됐다"고 말하기 전에 실제로 돌려 봐라.** 확인 못 한 것은 못 했다고 말하라.
- **연습 모드가 기본이다.** 실제 게시는 사용자가 확인 페이지를 보고 직접 실행할 때만. 자동 테스트로 실제 계정에 게시 금지. 테스트 게시가 꼭 필요하면 **사용자에게 먼저 묻고**, 동의받은 테스트 계정에만.
- **사용자 데이터를 테스트로 덮어쓰지 마라.** (`data/settings.json`, `data/history.json`, GitHub Secrets)
- **열쇠(토큰·키)는 채팅으로 받지 마라.** GitHub Secrets에 사용자가 직접 넣도록 `설정안내.md`로 안내하라. 로그·파일·확인 페이지 어디에도 출력 금지.
- GitHub 가입, 저장소 만들기, Secrets 넣기, Pages 켜기, Meta(인스타) 앱·토큰 발급은 **사용자가 직접 누르는 단계**다. 화면 이름·버튼 이름 그대로 번호 매긴 순서 안내문을 만들고, 대화에서는 한 단계씩 안내하라.

---

## 1. 한 줄 요약

매일 07:00(한국시간) GitHub Actions가 자동 실행 → 보도자료·뉴스 수집 → 카드 사진·캡션 생성 → 확인 페이지(GitHub Pages)에 게시 → 사용자가 아이패드·휴대폰으로 열어 확인 → **[인스타에 올리기]** → GitHub이 Instagram 공식 API로 캐러셀 게시 → 인스타 링크 기록.

---

## 2. 구조 · 비용 · 기술 스택

### 무료 구성
| 역할 | 서비스 | 비고 |
|---|---|---|
| 매일 자동 실행·게시 실행 | GitHub Actions | 공개 저장소는 무료 |
| 확인 페이지, 카드 사진 **공개 주소** | GitHub Pages | 인스타 API는 사진을 공개 URL로만 받는다. 비공개 저장소의 Pages는 유료 → **공개 저장소** |
| 열쇠 보관 | GitHub Secrets | 공개 저장소여도 밖에서 안 보임 |
| 게시 | Instagram 공식 API | 무료, 전문가 계정 필요 |
| 뉴스 헤드라인 | 네이버 검색 API(뉴스) | 무료 키 |
| 정책·세금 원문 | 정책브리핑(korea.kr) 보도자료 | 공공누리 확인 |

- **공개 저장소라 코드와 카드 사진은 주소를 아는 사람이 볼 수 있다**는 점을 사용자에게 안내(어차피 인스타에 올릴 내용). 게시 안 한 세트도 보인다.
- Python 3.11, `httpx`, `beautifulsoup4`, `feedparser`, `Pillow`. 글꼴 **Pretendard 파일을 저장소에 동봉**(클라우드 서버에는 한글 글꼴이 없다, OFL 라이선스 파일 포함).
- **`requirements.txt`는 ASCII만**(한글 주석 금지).

### 폴더 구조
```
cardnews/
  .github/workflows/make.yml       # 매일 07:00 KST(cron "0 22 * * *" UTC) + 수동 실행(원문 URL 입력 가능)
  .github/workflows/publish.yml    # 수동 실행 전용: 날짜·세트·모드(연습/실제)·확인문구·캡션 수정
  engine/sources_gov.py            # 정책브리핑 보도자료 수집·본문 추출
  engine/license.py                # 공공누리 유형 판별
  engine/embargo.py                # 보도시점(엠바고) 파싱
  engine/sources_news.py           # 네이버 뉴스 헤드라인
  engine/filter.py                 # 분야 키워드 점수·광고성 제외·중복 제외
  engine/splitter.py               # 원문 → 카드 단위 나누기(글자 변경 없음)
  engine/cards.py                  # Pillow 카드 그리기
  engine/caption.py                # 캡션·해시태그
  engine/instagram.py              # 게시·토큰 확인
  engine/history.py                # 기록
  engine/pages.py                  # 확인 페이지 HTML 생성
  docs/index.html                  # Pages 첫 화면(날짜 목록)
  docs/YYYY-MM-DD/index.html  set-1/ cards/01.jpg…  set.json  caption.txt
  fonts/Pretendard-*.otf  OFL.txt
  data/settings.json  history.json
  scripts/self_check.py  run_local.py
  requirements.txt  README.md  설정안내.md
```

---

## 3. 저작권 · 사실 원칙 (가장 중요, 절대 어기지 말 것)

### 3-1. 정부 보도자료 — 원문 그대로 사용
- 출처: 정책브리핑(korea.kr)의 부처별 보도자료 — **국토교통부, 기획재정부, 국세청, 행정안전부, 금융위원회** (+한국부동산원 보도자료는 공공누리 표시를 확인할 수 있을 때만). 목록 주소(RSS 또는 목록 페이지)는 **실제로 열어 확인**하고 `data/settings.json`에 둔다.
- **공공누리 판별(`license.py`)**: 본문 페이지의 공공누리 표시(이미지 alt, 파일명, 문구)를 읽어 유형을 정한다.
  - **제1유형(출처표시)만 자동 사용.**
  - 제2유형·제4유형(상업적 이용 금지): 중개사 홍보 계정은 상업적 이용 → 사용 불가
  - 제3유형(변경 금지): 카드로 나누는 것이 변경일 수 있어 사용 불가
  - 표시 없음·판별 실패: 사용 불가
  - 사용 불가는 후보 목록에 **"사용 불가: 공공누리 제2유형(상업 이용 금지)"**처럼 이유만 표시.
- **보도자료 속 사진·도표·첨부 이미지는 쓰지 않는다**(제3자 저작물일 수 있음). 글만 사용. 첨부 HWP/PDF는 v1에서 읽지 않는다.
- **카드 글자 = 원문 글자.** 요약·바꿔 쓰기·AI 사용 없음. `splitter.py` 결과를 `self_check`에서 검사: 공백·줄바꿈만 정리한 뒤 **모든 카드 본문이 원문의 부분 문자열**이어야 하고, 아니면 그 세트를 만들지 않고 이유를 기록한다.
- 문장 중간에서 자르지 않는다. 뒤를 생략하면 `(중략)`을 따로 붙이고 마지막 카드에 "전문은 출처 링크에서" 표시. `(중략)`·`전문은…`·출처·면책 같은 **프로그램이 붙이는 글은 본문과 다른 모양(작은 회색)**으로 구분.
- 출처 표기(마지막 카드 + 캡션): `출처: 국토교통부 「보도자료 제목」(2026.09.17), 정책브리핑 www.korea.kr — 공공누리 제1유형` + 원문 URL(캡션).

### 3-2. 신문 기사 — 본문 복사 금지
- 네이버 검색 API 뉴스(`https://openapi.naver.com/v1/search/news.json`, 헤더 `X-Naver-Client-Id`/`X-Naver-Client-Secret`, `sort=date`). 규격은 실제 문서로 확인.
- **사용하는 것: 기사 제목, 언론사 이름, 발행일, 원문 링크만.** `description`(요약문)과 본문은 카드·캡션·확인 페이지 어디에도 넣지 않는다(자체 검증으로 강제).
- 언론사 이름은 `originallink` 도메인 → 언론사 이름표(`settings.json`)로. 모르는 도메인은 도메인 그대로.
- "오늘의 부동산 뉴스" 헤드라인 카드: 한 카드에 기사 1건(제목·언론사·날짜), 최대 5건. 링크는 캡션에.
- 사람이 쓰는 **"한 줄 설명"**: publish 실행 화면 입력칸(기사별 `1|설명` 형식이 아니라 쉬운 방법을 설계 — 예: 칸 5개). 비우면 카드에 표시하지 않음. 한 줄 설명이 있으면 카드를 게시 직전 다시 그린다.

### 3-3. 사실 보호
- **엠바고(`embargo.py`)**: 보도자료의 `보도시점` 문구(예: `2026. 9. 18.(목) 조간`, `9.17.(수) 11:00`, `배포 즉시 보도`)를 읽어 공개 가능 시각을 정한다. 조간 = 전날 18:00이 아니라 **보수적으로 당일 06:00**, 석간 = 당일 12:00(규칙을 표로 문서화). 그 시각 전에는 확인 페이지에 "○월 ○일 ○시부터 올릴 수 있어요", **publish.yml에서도 다시 검사해 거부**. 못 읽으면 발표일 다음 날 06:00으로 보수 처리 + 안내.
- 제목·본문에 `개정안 / 추진 / 예정 / 입법예고 / 검토 / 발표(안)` 등이 있으면 표지에 **"확정 아님 · 정부안"** 배지. 세법은 국회 통과 전 내용일 수 있다.
- 표지에 **발표일**을 크게(본문의 "오늘·내일"은 원문 그대로 두되 기준일이 보이게).
- 마지막 카드 고정 면책: `정보 제공용 콘텐츠입니다. 세금·대출은 반드시 전문가와 상담하세요.`
- 광고성 제외: 제목에 `분양 / 모델하우스 / 견본주택 / 선착순 / 특별공급 오픈 / 투자 설명회` 등(목록은 설정) → 뉴스 후보에서 제외.
- 과장 표현을 프로그램이 만들어 넣지 않는다(표지 문구도 원문 제목 그대로).

---

## 4. 수집 · 선정 (`make.yml`, 매일)

1. 최근 24시간(주말 다음 날은 72시간) 보도자료 목록 수집 → 본문·발표일·보도시점·공공누리 유형 추출
2. 분야 키워드 점수(`settings.json`에서 수정 가능)
   - 정책: 공급, 재건축, 재개발, 청약, 전세, 월세, 임대차, 전세사기, 주택담보대출, DSR, LTV, 규제지역, 토지거래허가
   - 세금: 양도소득세, 종합부동산세, 취득세, 재산세, 보유세, 임대소득, 공시가격
   - 시장: 주간 아파트 가격동향, 거래량, 미분양, 실거래가
   - 제목 가중치 3, 본문 1. 기준 점수 미만 제외
3. 공공누리 제1유형만 → `history.json`에 이미 게시한 원문 URL 제외
4. 결과
   - **세트 1: 정책·세금 카드뉴스** — 점수 1위 보도자료(동점이면 최신)
   - **세트 2: 오늘의 부동산 뉴스 헤드라인** — 키워드 뉴스 중 광고성·중복(같은 제목 유사도 높음) 제외 최신 5건
   - 보도자료가 없는 날은 세트 2만. 둘 다 없으면 "오늘은 만들 내용이 없어요" 페이지
5. **후보 목록**(점수, 공공누리 유형, 사용 불가 이유)도 확인 페이지에 표시. 다른 보도자료로 다시 만들려면 make.yml 수동 실행의 `원문 URL` 칸에 붙여 넣기(버튼에서 실행 화면으로 바로 이동).
6. 하나가 실패해도 나머지는 진행. 결과는 확인 페이지 맨 위에 쉬운 말로(가져옴 / 제외 이유 / 실패).
7. 생성물은 `docs/날짜/`에 커밋 → Pages 반영. 30일 지난 카드 폴더는 정리(history는 유지).

---

## 5. 카드 규칙 (`splitter.py`, `cards.py`, AI 없음)

- **1080×1350px(4:5), JPEG(sRGB, 품질 90, 8MB 이하), 세트당 최대 10장**(인스타 캐러셀 한도, 문서로 확인).
- 구성
  1. **표지**: 분야 태그(정책/세금/시장/뉴스), 원문 제목, 부처명·발표일, 배지(확정 아님)
  2. **본문**(최대 8장)
  3. **마지막**: 출처, 면책, 중개사무소 정보(상호·대표·등록번호·전화 — `settings.json`, 비어 있으면 그 줄 생략. 임의로 채우지 말 것)
- 나누기 규칙
  - 보도자료의 항목 기호 `□ ○ ◦ - ※ ①` 구조를 살려 **`□` 한 항목 = 카드 한 장**, 하위 `○ -`는 들여쓰기.
  - 넘치면 **문장 단위**(`다.` `함.` `임.` 등 종결 뒤)로 다음 카드. 한 문장이 한 카드보다 길면 글자 크기를 최소 크기까지 줄이고, 그래도 넘치면 그 항목을 건너뛰고 `(중략)`.
  - 8장이 넘으면 앞 항목 우선, 뒤는 `(중략)`.
  - 담당부서·문의처·붙임 목록·"사진 설명" 줄은 제외(제외는 허용, 바꾸기는 금지).
- 글자: Pretendard. 제목은 실제 글자 폭을 재서 넘칠 때만 줄이고 두 줄까지. 본문 최소 34px.
- 강조: **숫자·%·금액·날짜만 색**(글자는 그대로).
- 디자인: 색(주색·배경·글자), 로고 이미지(선택), 계정 이름은 `settings.json`. 기본은 차분한 톤(제안서 색 `#9E9577` 계열 재사용 가능). 쪽 번호 `2/7`.
- **캡션(`caption.py`)**: 원문 제목 + 원문 첫 문단(제1유형일 때만, 원문 그대로) + `▶ 원문 보기 URL` + 출처 + 면책 + 고정 해시태그(`settings.json`). **2,200자·해시태그 30개 한도 검사**(넘치면 첫 문단을 문장 단위로 줄임). 뉴스 세트 캡션은 기사 제목·언론사·링크 목록만.

---

## 6. 확인 페이지 (GitHub Pages, 아이패드·휴대폰용)

- 첫 화면: 날짜 목록(최신 위), 각 날짜에 세트 요약·게시 여부·인스타 링크. 맨 위에 **토큰 만료 경고**, 예약 실행이 멈췄을 때 경고.
- 날짜 화면(세트마다)
  - 카드 **손가락으로 넘겨보기**(아이패드 768~1024px, 휴대폰 375px 모두), 누르면 크게
  - **원문 대조**: 원문 전체를 보여주고 카드에 쓰인 부분을 형광 표시, 뺀 부분은 흐리게
  - 캡션 보기 + [캡션 복사]
  - 표시: 공공누리 유형, 엠바고 시각("지금 올릴 수 있어요" / "○시부터"), 확정 아님 배지, 이미 게시됨
  - **[인스타에 올리기]** → 그 날짜·세트가 들어간 `publish.yml` 실행 화면(`https://github.com/{계정}/{저장소}/actions/workflows/publish.yml`)으로 이동. 실행 화면에서 입력할 값(날짜·세트 번호)을 **버튼 위에 크게 보여주고 복사 버튼** 제공(GitHub 실행 화면은 값 미리 채우기가 안 될 수 있으니 확인 후 설계). GitHub 모바일 앱/사파리 로그인 상태에서 "Run workflow"를 누르는 순서를 그림으로 안내.
  - [다른 보도자료로 다시 만들기] → make.yml 실행 화면 + 후보 URL 복사 버튼
- **정적 페이지이므로 토큰·키를 절대 넣지 않는다.** 페이지에서 GitHub API를 직접 호출하는 방식(개인 토큰을 브라우저에 저장)도 쓰지 않는다.
- 캐시 방지: 사진·JS·CSS 주소에 `?v=생성시각`.
- 알림: 만들기·게시가 끝나면 GitHub 알림(모바일 앱 푸시). 실패 시 Actions가 실패로 끝나야 알림이 온다. 텔레그램 등 추가 알림은 선택 기능으로 README에만 언급.
- 모든 입력 안내 예시는 `예) …` 형식.

---

## 7. 인스타 게시 (`publish.yml`, `instagram.py`)

- **공식 Instagram API만.** 아이디·비밀번호 로그인 자동화, 비공식 라이브러리(instagrapi 등), 브라우저 자동 로그인 **금지**(계정 정지 위험. 네이버 블로그 자동화 때 로그인 상태가 조용히 만료된 적도 있다).
- 준비(사용자, `설정안내.md`): 인스타 계정을 **전문가 계정(비즈니스/크리에이터)**으로 전환 → Meta 개발자 앱 → 게시 권한 → 장기 토큰 → GitHub Secrets `IG_USER_ID`, `IG_ACCESS_TOKEN`, `NAVER_CLIENT_ID`, `NAVER_CLIENT_SECRET`. **연결 방식(Instagram 로그인 방식 / 페이스북 페이지 연결 방식)은 현재 Meta 문서로 확인해 비개발자에게 더 쉬운 쪽**을 고르고 이유를 보고.
- 실행 입력: `날짜`, `세트 번호`, `모드`(연습/실제, 기본 연습), `확인 문구`(실제일 때 `올립니다` 입력해야 진행), `캡션 수정`(비우면 자동 캡션), 뉴스 `한 줄 설명` 칸.
- 게시 전 검사(하나라도 실패하면 게시 안 하고 이유 표시): 엠바고 시각 지남 · 공공누리 제1유형 · 같은 원문 게시 기록 없음 · 카드 사진 Pages 주소가 실제로 열림(200, `image/jpeg`, 몇 분 기다리며 재시도 — Pages 반영 지연) · 캡션 한도 · 게시 한도(`content_publishing_limit`) 여유 · 토큰 유효.
- 흐름(규격은 문서로 확인): 사진마다 캐러셀 항목 컨테이너 → CAROUSEL 컨테이너(children, caption) → **상태가 FINISHED 될 때까지 대기**(시간 제한·재시도) → 게시 → 게시물 링크 조회.
- **연습 모드**: 인스타 API는 토큰 확인(읽기)만, 게시 호출 없음. "실제였다면 이렇게 올라갑니다"(사진 수·캡션·검사 결과)를 기록·페이지에 표시.
- 결과: `history.json`에 날짜·세트·원문 URL·모드·인스타 링크·시각 기록, 확인 페이지 갱신 커밋.
- **토큰 만료**: 장기 토큰(보통 60일, 문서 확인). make.yml이 매일 남은 기간 확인 → 갱신 가능하면 갱신 방법 확인 후 구현(Secrets를 코드로 갱신하려면 추가 권한이 필요하니 가장 단순하고 안전한 방법을 제시), 7일 전부터 확인 페이지 맨 위 경고 + Actions 경고.

---

## 8. 실전 함정 모음 (반드시 반영)

1. **Actions 예약 실행은 UTC**(07:00 KST = `0 22 * * *` 전날 UTC)이고 몇 분~수십 분 늦을 수 있다. 공개 저장소에 **60일간 활동이 없으면 예약 실행이 자동으로 꺼진다** → 매일 생성 커밋으로 유지, 꺼졌으면 다시 켜는 방법 안내.
2. **열쇠 노출 금지**: `print`·예외 메시지·HTTP 오류 본문에 토큰이 섞이지 않게(URL 쿼리에 토큰이 들어가면 오류 로그에 찍힌다 → 가림 처리). 공개 저장소 Actions 로그는 누구나 본다.
3. 외부 기여자의 PR로 워크플로가 Secrets를 쓰지 못하게(`pull_request_target` 사용 금지, publish는 `workflow_dispatch`만).
4. Actions 커밋 권한: `permissions: contents: write`, 동시에 두 워크플로가 커밋하면 충돌 → `concurrency` 그룹 + 커밋 전 `git pull --rebase`.
5. **Pages 반영 지연** → 게시 전 사진 주소 확인·재시도.
6. 네이버 뉴스 결과의 `<b>`, `&quot;` 등 HTML 태그·엔티티 제거. API 하루 호출 한도 확인.
7. 정책브리핑 구조가 바뀌면 수집이 조용히 0건이 된다 → 목록은 있는데 본문 추출 0이면 "구조가 바뀐 것 같아요" 실패로 알리고, 원문 URL 수동 입력으로 계속 쓸 수 있게.
8. 글꼴 동봉 안 하면 한글이 네모(□)로 깨진다. 보도자료 기호 `□ ○ ◦ ※ ①`이 글꼴에 있는지 확인, 없으면 대체 글꼴.
9. 본문 추출 시 `&nbsp;`·전각 공백·줄바꿈 차이로 "원문 부분 문자열" 검사가 거짓 실패 → 비교 전 같은 방식으로 정리(글자 자체는 바꾸지 않음).
10. 컨테이너가 FINISHED 되기 전 게시하면 실패. 이미지 비율 4:5 범위 밖이면 거부된다.
11. `requirements.txt` 한글 금지.
12. 테스트가 `data/settings.json`·`history.json`을 덮어쓰지 않게(테스트는 임시 폴더).
13. 시간대: 모든 날짜 판단은 `Asia/Seoul`로(클라우드 서버는 UTC).
14. `scripts/run_local.py`: 노트북에서도 make 과정을 똑같이 돌려 `docs/`를 만들어 볼 수 있게(개발·검증용, 게시 기능은 연습 모드만).

### 8-1. 실제로 만들며 확인한 사실 (2026-09-17, v1 구현 `클로드테스트\cardnews`)
15. **정책브리핑 RSS는 중단됨**("정책브리핑 RSS 서비스 제공 중단 안내"). 목록 화면 `/briefing/pressReleaseList.do?repCodeType=정부부처&repCode=코드&pageIndex=N`(10건/쪽)을 읽는다.
16. 부처 코드: 국토교통부 `A00006`, 재정경제부 `A00041`, 기획예산처 `A00040`, 국세청 `B00004`, 행정안전부 `A00031`. **기획재정부는 재정경제부·기획예산처로 개편**됨. 금융위원회는 '정부부처' 목록에 없음.
17. 본문 원문은 페이지가 아니라 **첨부 PDF/HWPX**에 있다(페이지 본문은 문서 뷰어 iframe). 공공누리 표시는 "**텍스트에 한하여** 공공누리 제1유형" → 글만 사용.
18. 첨부 파일 이름에 `260918(조간)`이 붙어 **보도시점 전에 먼저 올라온다.** 보도시점 줄은 `2026. 9. 17.(목) 11:00 이후(9. 18.(금) 조간) / 배포 : …` 형식 → 가장 이른 적힌 시각.
19. PDF 글 순서: 기본 추출은 제목이 본문 뒤에 나온다 → **줄 위치(y) 순으로 정렬**. **표(2줄×2칸 이상) 안의 글은 빼야** 한다(풀어 쓰면 순서가 섞여 뜻이 바뀜). PDF 줄 끝 공백이 있으면 띄어 잇고, 없으면 붙여 잇는다.
20. 머리글 "**보도자료**"를 "보도시점"으로 잘못 읽는 함정 → `보도` 뒤에 `시점/일시/일` 또는 `:`이 있어야 인정.
21. 키워드 부분일치 함정: `인재개발원`⊃재개발, `공급망`⊃공급 → 제외 목록으로 먼저 지운 뒤 점수.
22. Pretendard에 없는 글자: `➊❶`(직접 동그라미+숫자로 그림), 반각 괄호 `｢｣`(NFKC로 「」 모양), 한자 `同`(Noto CJK/맑은고딕으로 대신 그림). **없는 글자 판별은 `͸` 글리프와 비교**. 저장 글자는 원문 그대로.
23. 각주(`*`)가 혼자 다음 카드로 떨어지지 않게 앞 문단과 함께 넘기고, 둘 다 들어가는 짧은 카드는 합친다(번호 소제목으로 시작하는 카드는 합치지 않음).
24. **한국 정부 사이트는 해외 접속을 막는다.** GitHub Actions(미국)에서 korea.kr·molit.go.kr·moef.go.kr·nts.go.kr·data.go.kr·eum.go.kr 모두 시간 초과, 네이버 API·Instagram API는 정상. → **정부 보도자료 카드는 노트북(윈도우 예약 작업: 매일 07:00 + 로그인 3분 뒤, 놓친 실행 따라잡기)이 만들어 push**, GitHub은 뉴스·확인 페이지·게시. 노트북은 `docs/날짜/set-1`·`run-gov.json`만, GitHub은 나머지만 써서 충돌 없음. 노트북 push가 오면 `pages.yml`이 확인 페이지 재생성.
25. 예약 작업 등록 `.ps1`은 **ASCII만**(Windows PowerShell 5.1이 BOM 없는 UTF-8 한글을 깨뜨려 파싱 오류).


---

## 9. 검증 체크리스트 (`scripts/self_check.py` + 실제 실행)

- [ ] 모든 보도자료 카드 본문이 원문의 부분 문자열, 문장 중간 잘림 0, 제외 줄(담당부서 등) 처리
- [ ] 공공누리 제1·2·3·4유형·표시 없음 샘플 페이지 판별
- [ ] 엠바고 문구 여러 형태(조간·석간·시각·배포 즉시·못 읽음) → 공개 시각, 그 전에 publish 거부
- [ ] 뉴스 세트의 카드·캡션·페이지에 `description` 문장이 한 글자도 없음
- [ ] 광고성 제목 제외, 같은 기사 중복 제외, 이미 게시한 원문 제외
- [ ] 카드 1080×1350 JPEG, 10장 이하, 긴 제목 두 줄 줄임, 기호 글꼴 정상
- [ ] 캡션 2,200자·해시태그 30개 한도
- [ ] **실제**: 최근 국토교통부 보도자료 1건 → 카드 세트·캡션·확인 페이지. 카드 사진을 직접 열어 보고 글자 깨짐·넘침 없음 확인
- [ ] **실제**: 확인 페이지를 휴대폰 폭(375px)·아이패드 폭(820px)으로 열어 넘겨보기·원문 대조·복사 버튼 동작
- [ ] **실제**: GitHub에서 make.yml 수동 실행 → Pages 반영 → publish.yml **연습 모드** 실행 → 검사 결과 기록
- [ ] **실제(사용자에게 묻고 동의한 경우만)**: 테스트 인스타 계정에 1회 실제 게시 → 링크 기록
- [ ] Actions 로그·저장소 파일 어디에도 토큰이 없음(검색으로 확인)
- [ ] 확인한 것 / 못 한 것을 나눠 쉬운 말로 보고

---

## 10. 개발 순서

1. 정책브리핑·네이버 API·Instagram API **실제 문서·페이지 확인** → 확인한 주소·규격을 README에 기록
2. `sources_gov.py` → `license.py` · `embargo.py` → `filter.py`
3. `splitter.py` → `self_check.py`(부분 문자열 검사부터)
4. `cards.py`(Pillow) → `caption.py` → `sources_news.py`
5. `pages.py` + `docs/` 확인 페이지 → `run_local.py`로 노트북에서 전체 확인
6. `make.yml` → GitHub에서 실행 → Pages 확인
7. `instagram.py`(연습 모드부터) → `publish.yml`
8. `README.md`, `설정안내.md`(GitHub 가입부터 Secrets·Pages·Meta 토큰까지 번호 순서)
9. §9 전부 확인 후 보고

---

## 부록: 사용자 선호 (대화에서 확정)

- 설명·안내는 쉬운 한국어, 결과 중심, 한 번에 하나씩.
- 주제: 부동산 **정책·세금·뉴스**만. 실제 발표·기사만, 지어낸 내용 없음.
- 원문 사용: **정부 보도자료(공공누리 제1유형)는 원문 그대로**, **신문 기사는 제목·언론사·날짜·링크만**(본문 복사는 저작권 문제로 하지 않기로 함).
- 게시: 자동으로 만들고 **사람이 확인한 뒤 올리기**. 연습 모드 기본, 실제 게시 전 한 번 더 확인.
- AI 요약 **사용 안 함**(비용 0원, 원문 그대로 나누기만).
- 실행: **클라우드(GitHub) + 아이패드·휴대폰에서 확인**, 노트북이 꺼져 있어도 동작, 비용 0원(공개 저장소).


---
---

# ▶ v2 — 실제 구축 기록 (2026-09-17 ~ 09-18)

> 위 v1 명세로 실제로 만들며 바뀐 결정, 겪은 오류와 해결, 현재 상태, 다음 작업 계획. **다시 만들거나 이어서 작업할 때는 이 v2가 우선**이다.

## A. 현재 상태 (2026-09-18 00:30 기준)

| 항목 | 내용 |
|---|---|
| 코드 위치 | `C:/Users/JY/클로드테스트/cardnews` (git) |
| GitHub 저장소 | `butonco00-ux/cardnews` (Public) |
| 확인 페이지 | https://butonco00-ux.github.io/cardnews/ |
| 인스타 계정 | @buto.story — 연결 확인(연습 모드만), **실제 게시는 아직 0건** |
| GitHub Secrets | `IG_ACCESS_TOKEN`, `NAVER_CLIENT_ID`, `NAVER_CLIENT_SECRET` (IG_USER_ID는 불필요: 토큰으로 자동 조회) |
| 노트북 예약 작업 | 윈도우 작업 스케줄러 `CardnewsGovDaily` — 매일 07:00 + 로그인 3분 뒤, 놓친 실행 따라잡기, 기록 `logs/laptop.log` |
| 자체 검사 | `python scripts/self_check.py` → 74/74 통과 |

## B. v1에서 바뀐 결정 (사용자 확인 완료)

1. **실행 구조: 클라우드 단독 → 노트북 + GitHub 나눠 쓰기**
   - GitHub Actions(미국)에서 korea.kr·molit.go.kr·moef.go.kr·nts.go.kr·data.go.kr·eum.go.kr 모두 **시간 초과**(해외 차단). 네이버 API·Instagram API는 정상.
   - **노트북**: `scripts/laptop_daily.py` → 정부 보도자료 카드 `docs/날짜/set-1` + `run-gov.json` + `releases.json` push
   - **GitHub** `make.yml`(07:00): 뉴스 카드 `set-2` + `run-news.json` + 토큰 확인 + 확인 페이지
   - **GitHub** `pages.yml`: 노트북 push가 오면 확인 페이지 재생성, 늦게 온 보도자료로 뉴스 카드에 정부 원문 다시 붙이기
   - **GitHub** `publish.yml`: 수동 실행(날짜·세트·스타일·모드·확인문구·캡션·한마디 5칸)
   - 노트북·GitHub가 **서로 다른 파일만 쓰게** 나눔. 확인 페이지(index.html) 충돌은 `scripts/save_and_push.sh`가 최신 내용 받은 뒤 다시 만들어 저장.
   - 노트북이 꺼진 날은 보도자료 카드만 빠짐(확인 페이지에 안내).
2. **네이버 뉴스 검색 API → NAVER API HUB**
   - 개발자센터 검색 API는 **2026-07-31 신규 신청 중단**, 2027-06-30 기존 키 종료. 신규는 네이버 클라우드 **NAVER API HUB**(한시적 무료, 유료 전환 시 사전 공지, 검색 월 775,000건).
   - 주소 `https://naverapihub.apigw.ntruss.com/search/v1/news`, 헤더 `X-NCP-APIGW-API-KEY-ID` / `X-NCP-APIGW-API-KEY`, `format=json`. 예전 방식도 자동 시도.
3. **뉴스 카드에 '정부 발표 원문' 붙이기**(사용자 선택, 0원): 같은 소식의 보도자료가 있으면 보도자료 첫 문단 1~2문장(공공누리 제1유형, 글자 그대로)을 상자로. 기사 문장은 여전히 0글자. 붙인 보도자료가 보도시점 전이면 뉴스 게시도 거부.
4. **뉴스 고르기**: 언론사 **7곳만** — 한국경제, 한국주택경제(arunews.com), 매일경제, 헤럴드경제, 아시아경제, 연합뉴스, 세계일보(`settings.news_press_only`). 연예·예능 제목 제외, 관련도 우선, **같은 소식은 1건**(토허구역=토지거래허가구역 같은 줄임말 묶기), 언론사당 1건.
5. **보도자료 부처 4곳**: 국토교통부 `A00006`, 재정경제부 `A00041`(구 기획재정부, 세제), 금융위원회 `C00003`(대출), 행정안전부 `A00031`(지방세). 국세청 `B00004`는 뺌(필요하면 settings에 추가).
6. **카드 스타일 2종을 매일 모두 만들고 올릴 때 고름**
   - 스타일 1(기본): 베이지 표지·흰 본문·갈색 숫자 강조 → `cards/`
   - 스타일 2(Buto): **1번과 같은 배치**, 색만 회색 `#EDEBE8`·검정 `#111111`·노랑 `#EEEA5E` → `cards-buto/`
     - 표지 제목의 숫자 든 줄(없으면 첫 줄)에만 노란 형광펜, **2장부터 노란색 없음**(본문 `#4A4A4A`, 숫자 검정)
     - 분야 태그 = 둥근 알약(검정 바탕·흰 글씨)
     - 맨 아래: 구분선 + 왼쪽 `footer_note` + 오른쪽 **Buto.** 로고(Pretendard Black), 쪽 번호·출처는 구분선 위
   - (중간에 만든 별도 Buto 레이아웃은 "안 예쁘다"는 의견으로 폐기)
7. **카드 문구 정리(사용자 요청)**
   - 마지막 출처 카드: "출처 + 부처 보도자료 「제목」(날짜)"만. 정책브리핑 주소·공공누리·안내문·면책 문구는 **카드에서 빼고 캡션에만**
   - 뉴스 세트: 마지막 '알려드려요' 장 삭제(표지 + 기사 카드), 카드 아래 '원문 링크는 캡션에' 글자 삭제. 캡션에는 기사 링크·정부 원문 링크 유지
8. **인스타 연결**: Meta 앱 사용 사례 "Instagram에서 메시지 및 콘텐츠 관리" → 왼쪽 **Instagram 로그인이 포함된 API 설정** → "2. 액세스 토큰 생성" → 계정 추가. `instagram_business_content_publish` 권한 추가 필요.

## C. 겪은 오류와 해결 (다시 만들 때 반드시 반영)

| # | 증상 | 원인 | 해결 |
|---|---|---|---|
| 1 | 정책브리핑 RSS 404 | RSS 서비스 중단 | 목록 화면 파싱 |
| 2 | 보도자료 본문이 페이지에 없음 | 원문은 첨부 PDF/HWPX | PyMuPDF로 PDF, 없으면 HWPX XML |
| 3 | 카드에 표 글이 뒤섞임, 제목이 본문 뒤에 나옴 | PDF 기본 추출 순서·표 | 줄 y좌표 정렬, **2줄×2칸 이상 표 제외** |
| 4 | ➊ ｢ ｣ 同 이 ≣ 로 깨짐 | Pretendard에 없는 글자 | `\u0378` 글리프와 비교해 없는 글자 판별 → ➊는 동그라미 직접 그림, ｢｣는 NFKC, 한자는 Noto CJK(Actions에 `fonts-noto-cjk` 설치)/맑은고딕 |
| 5 | 올릴 수 있는 자료가 "내일 06:00부터"로 막힘 | 머리글 "**보도**자료"를 보도시점으로 읽음 | `보도` 뒤에 `시점/일시/일` 또는 `:` 필수 |
| 6 | 금융위 자료가 다음 날로 밀림 | "배포시" 표현 미인식 | `즉시/배포 시/배포 후` → 배포일 즉시 |
| 7 | 금융위 자료 카드가 "원문에 없는 글자"로 차단 | 쪽을 넘는 문장을 이을 때 뺀 쪽 번호("- 3 -")가 대조 원문엔 남아 있음 | 대조할 때도 쪽 번호·머리글 줄을 빼고 비교 |
| 8 | 엉뚱한 보도자료가 후보 | '인재**개발**원'⊃재개발, '**공급**망'⊃공급 | 오탐 단어 목록 먼저 제거 |
| 9 | 각주(*)만 한 장에 혼자 | 넘침 처리 | 각주는 앞 문단과 함께 넘기기, 짧은 카드 합치기 |
| 10 | 뉴스에 연예 기사·같은 소식 4건 | '아파트' 포함 제목, 제목 표현 차이 | 연예 단어 제외, 핵심 단어 겹치면 같은 소식 |
| 11 | GitHub에서 정부 사이트 전부 시간 초과 | 해외 IP 차단 | B-1 나눠 쓰기 |
| 12 | 네이버 등록 화면에 '검색' 없음 | 개발자센터 신규 중단 | NAVER API HUB |
| 13 | 네이버 401 | 비밀 보관함 Name 칸에 값 입력/값 오류 | 안내 후 재입력, 거절 이유 로그, 앞뒤 공백 제거·ID/Secret 바뀜 자동 시도 |
| 14 | 인스타 '개발자 역할 권한 부족' | 계정 추가 조건(테스터·권한) | 앱 역할 → Instagram 테스터, instagram.com/accounts/manage_access 수락, 권한 추가 |
| 15 | Actions 실패: index.html 병합 충돌 | 노트북 push 직후 뉴스 작업이 같은 페이지 저장 | `save_and_push.sh`로 충돌 시 재생성 |
| 16 | 예약 작업 등록 실패 | PowerShell 5.1이 BOM 없는 UTF-8 한글 스크립트를 깨뜨림 | `register_task.ps1` **ASCII만** |
| 17 | 코드에 줄바꿈이 섞여 문법 오류 | bash heredoc에서 `\n`이 실제 줄바꿈으로 들어감 | 패치 파일(Write)로 적용하거나 `chr(10)` 사용, 적용 후 반드시 self_check |
| 18 | 확인 서버를 켜다 블로그 앱이 켜짐 | 상위 폴더 launch.json 이름 착각 | `cardnews-preview` 설정 추가, 이름 확인 후 실행 |
| 19 | Actions 수동 실행 400 | 한글 JSON을 curl 인자로 보냄(인코딩) | UTF-8 파일로 `--data-binary @파일` |

## D-완료. 법령·생활법령 세트 추가 (2026-09-18 저녁 반영 완료)

- **세트 3 법령**: 국가법령정보센터 OPEN API. 목록 `lawSearch.do?target=law&efYd=…~…|ancYd=…~…&sort=efdes`(한글 검색어는 **UTF-8 %인코딩**, EUC-KR 이면 0건), 본문 `lawService.do?MST=…` 의 `법령.제개정이유.제개정이유내용`. **타법개정은 제외**(개정이유가 다른 법 이야기). 카드 = 법령명·시행일·부처 + 개정이유 앞 문장 원문. OC 는 `settings.law_oc`/`LAW_OC`(없으면 공용 `test`).
- **세트 4 생활법령**: 찾기쉬운 생활법령정보 부동산/임대차 27개 주제 → 항목 하나를 매일 순환(이미 쓴 주소 제외). 본문 `div#ovDiv`, 조문 인용이 줄로 쪼개진 것은 이어 붙임. 저작권: 법제처 정책상 영리 목적 포함 자유 이용(제3자 저작물 제외).
- **기호가 없는 글도 카드로**: splitter 가 □·ㅇ 없는 글은 첫 줄을 제목으로, 나머지를 본문 항목으로.
- 노트북이 세트 1·3·4 를, GitHub 이 세트 2 와 확인 페이지를 만든다. publish 세트 번호는 1~4.
- 자체 검사 79개 통과, 2026-09-18 실제 실행으로 4세트 모두 확인 페이지 반영 확인.

## D-2. 남은 계획

사용자가 추가하고 싶은 자료:

| 자료 | 확인 결과(2026-09-18) | 쓰는 방법(안) | 필요한 것 |
|---|---|---|---|
| ~~국가법령정보센터~~ (완료) | Open API `https://www.law.go.kr/DRF/lawSearch.do?target=law&type=JSON` 응답 확인(OC=test로 호출됨). 법령은 저작권 보호 대상 아님(저작권법 제7조) | 새 세트 **"이번 주 시행되는 부동산 법령"**: 주택·임대차·세법 관련 법령명·시행일·공포일, 조문은 원문 그대로 일부 | **OC(이용자 ID) 발급**: open.law.go.kr 회원가입 후 API 신청(무료). 해외 차단 여부 미확인 → 노트북에서 수집 |
| ~~찾기쉬운 생활법령정보~~ (완료) | 저작권정책: 법제처 법령정보는 **영리 목적 포함 자유 이용**(제3자 저작물 제외) | 새 세트 **"알아두면 좋은 부동산 법"**: 임대차·매매 테마 Q&A를 원문 그대로 카드로(주 1~2회 순환) | 목록·본문 구조 파악, 제3자 저작물 표시 확인, 출처 표기 문구 |
| **정책브리핑 정책풀이집·Q&A** | 정책풀이집 주소(molit.go.kr/policy/main.jsp)는 **현재 열리지 않음(리다이렉트 반복 후 404)**. 정책브리핑 내 부동산 Q&A 전용 목록은 아직 못 찾음 | 찾으면 생활법령과 같은 Q&A 카드 | 사용자와 현재 주소 확인 필요 |
| 4대 부처 보도자료 | **반영 완료**(국토·재경·금융위·행안) | — | — |
| 뉴스 7개 언론사 | **반영 완료** | — | — |

진행 순서(안):
1. 사용자에게 확인: 새 세트를 **매일** 만들지 **요일별 순환**(예: 월 법령·수 생활법령)인지, 스타일 1·2 모두인지
2. 국가법령정보센터 OC 발급 안내(사용자가 직접) → 노트북 수집기 `engine/sources_law.py`
   - 최근 공포·시행 예정 법령 중 키워드(주택, 임대차, 부동산, 공인중개사, 지방세, 소득세, 종합부동산세, 토지) 필터
   - 카드: 법령명 · 공포일 · 시행일 · 개정 이유/주요내용(원문 그대로) · 출처(국가법령정보센터)
3. 생활법령 수집기 `engine/sources_easylaw.py` — 부동산/임대차 분류의 Q&A 목록 → 원문 그대로 카드, 이미 올린 항목 제외
4. 정책풀이집/정책 Q&A는 현재 주소 확인 후 결정
5. 모든 새 수집기는 **노트북 실행**(해외 차단 가능성), 결과는 `docs/날짜/set-3`, `set-4` + `run-law.json` 등 **노트북 전용 파일**
6. self_check에 법령·생활법령 파싱 샘플 추가 → 실제 1회 실행 → 확인 페이지 확인 → 연습 모드 게시(사용자 동의 후)
7. 첫 **실제 게시**는 사용자와 함께 확인하며 진행

## E. 매일 흐름 요약 (지금)
1. 07:00 노트북 → 보도자료 카드(스타일 1·2) + 보도자료 목록 push
2. 07:00 GitHub → 뉴스 카드(스타일 1·2, 정부 원문 붙임) + 확인 페이지
3. 사용자: 아이패드·휴대폰으로 확인 페이지 → [인스타에 올리기] → GitHub 실행 화면에서 날짜·세트·**스타일**·모드(연습/실제)·`올립니다`
4. 토큰 60일 만료 7일 전 경고. 네이버 API HUB 유료 전환 공지 확인.
