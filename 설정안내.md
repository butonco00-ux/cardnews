# 처음 설정 안내 (한 번만 하면 돼요)

> **왜 노트북도 쓰나요?** 정책브리핑·국토교통부·국세청 등 한국 정부 사이트가 해외 서버(GitHub)의 접속을 막아요(2026-09-17 확인).
> 그래서 **정부 보도자료 카드는 노트북이 매일 아침 만들어 올리고**, 뉴스 카드·확인 페이지·인스타 올리기는 GitHub이 해요.
> 노트북이 꺼져 있던 날은 켜서 로그인하면 3분 뒤 자동으로 따라잡아요.

순서대로 하나씩 하면 돼요. 막히면 그 단계 번호를 알려 주세요.
열쇠(토큰·비밀번호)는 **채팅에 붙여 넣지 말고** 아래 안내대로 GitHub의 비밀 보관함에만 넣어 주세요.

---

## 1단계. GitHub 가입
1. https://github.com 접속 → 오른쪽 위 **Sign up**
2. 이메일·비밀번호·아이디 입력 후 가입 (무료 요금제 Free)
3. 휴대폰·아이패드에 **GitHub 앱**을 설치하고 같은 아이디로 로그인해 두면, 나중에 [인스타에 올리기]가 편하고 결과 알림도 받아요.

## 2단계. 저장소 만들고 파일 올리기
1. 로그인 후 오른쪽 위 **＋ → New repository**
2. Repository name: `cardnews` (원하는 이름 가능)
3. **Public** 선택 ← 무료로 확인 페이지를 쓰려면 공개여야 해요
   - 공개라서 프로그램 코드와 카드 사진은 주소를 아는 사람이 볼 수 있어요(어차피 인스타에 올릴 내용이에요).
   - 열쇠는 3·4단계의 비밀 보관함에 넣기 때문에 밖에서 보이지 않아요.
4. **Create repository**
5. 파일 올리기: 저장소 화면의 **uploading an existing file** 링크 → `cardnews` 폴더 **안의 모든 것**(`.github` 폴더 포함, `preview` 폴더는 빼기)을 끌어다 놓기 → 아래 **Commit changes**
   - `.github` 폴더가 안 보이면 윈도우 탐색기 **보기 → 숨긴 항목**을 켜세요.
   - 어려우면 "저장소에 대신 올려줘"라고 말씀해 주세요.

## 3단계. GitHub 설정 3가지
저장소 화면 위쪽 **Settings**에서:
1. 왼쪽 **Actions → General** → 맨 아래 **Workflow permissions** → **Read and write permissions** 선택 → **Save**
2. 왼쪽 **Pages** → Source: **Deploy from a branch** → Branch: **main**, 폴더 **/docs** → **Save**
   - 몇 분 뒤 위쪽에 `https://아이디.github.io/cardnews/` 주소가 나와요. **이게 확인 페이지 주소**예요. 아이패드·휴대폰 홈 화면에 추가해 두세요(사파리 공유 버튼 → 홈 화면에 추가).
3. 왼쪽 **Secrets and variables → Actions** → **New repository secret** 로 아래 4단계·5단계의 열쇠를 넣어요.

## 4단계. 인스타 열쇠 넣기 (IG_ACCESS_TOKEN 하나만)
1. 인스타 앱 → 프로필 → ☰ → **계정 유형 및 도구 → 프로페셔널 계정으로 전환** (비즈니스 또는 크리에이터)
2. PC에서 https://developers.facebook.com/apps → **앱 만들기**
3. 사용 사례에서 **Instagram에서 메시지 및 콘텐츠 관리**(영어: *Manage messaging and content on Instagram*) 선택 → 끝까지 **다음** → 앱 만들기
4. 앱 대시보드 왼쪽 **사용 사례** → 그 사용 사례의 **맞춤 설정**(Customize)
   - **권한**(Permissions)에서 `instagram_business_content_publish` 옆 **추가**를 눌러요(게시에 꼭 필요)
5. 같은 화면의 **Instagram 로그인을 통한 API 설정**(API setup with Instagram login) → **액세스 토큰 생성**(Generate access tokens) 칸
   - **계정 추가**(Add account) → 계속 → 팝업에서 인스타 로그인 → 허용 → 저장
   - 추가된 계정 줄 오른쪽 **토큰 생성**(Generate token) → 긴 글자가 나오면 **복사**
6. 3단계 비밀 보관함에 **이름 `IG_ACCESS_TOKEN`** ← 복사한 긴 글자
   - 숫자 사용자 ID(`IG_USER_ID`)는 **안 넣어도 돼요.** 프로그램이 토큰으로 알아내요.
7. 앱이 "개발 모드"여도 **여기서 추가한 내 계정**에는 게시할 수 있어요.

> **토큰은 60일마다 만료돼요.** 확인 페이지 맨 위에 만료 7일 전부터 경고가 나와요. 그때 4단계 4~5번을 다시 해서 `IG_ACCESS_TOKEN` 값을 새로 넣어 주세요(이름 옆 연필 → 값 바꾸기).
> 자동으로 갱신하게 하려면 7단계(선택)를 해 주세요.

## 5단계. 네이버 뉴스 열쇠 넣기 (뉴스 헤드라인용)
> 네이버 개발자센터의 검색 API는 **2026년 7월 31일부터 신규 신청이 막히고** 네이버 클라우드의 **NAVER API HUB**로 옮겨졌어요.
> 지금은 **한시적 무료**이고 나중에 유료 요금제가 생길 예정이라고 안내돼 있어요. 유료가 되면 이 세트만 끄면 돼요(정책·세금 카드는 그대로).

1. https://www.ncloud.com 에서 **회원가입**·로그인 (네이버 아이디로 가입 가능, 결제수단 등록을 요구할 수 있어요)
2. **콘솔** → 왼쪽 **Services**(전체 서비스) → **Application Services** → **NAVER API HUB**
3. 왼쪽 **Application** → **[Application 등록]** → API 목록에서 **검색 → 뉴스** 선택 → 이름 `카드뉴스` → **[완료]**
4. 만든 Application의 **[인증 정보]** → **Client ID**, **Client Secret** 확인
5. 3단계 비밀 보관함(https://github.com/butonco00-ux/cardnews/settings/secrets/actions/new)에 두 번:
   - 이름 `NAVER_CLIENT_ID` ← Client ID
   - 이름 `NAVER_CLIENT_SECRET` ← Client Secret
   - 이 열쇠가 없으면 뉴스 세트만 빠지고, 정책·세금 카드는 그대로 만들어져요.

## 6단계. 내 정보 넣기 (`data/settings.json`)
저장소에서 `data/settings.json` 파일 → 연필(✏️) 버튼 → 아래만 바꾸고 **Commit changes**
- `"account_name"`: 인스타 계정 이름 예) `"@○○부동산"`
- `"office"`: 상호·대표·등록번호·전화·주소 (비워 두면 카드에 안 나와요. 지어낸 정보는 넣지 마세요)
- `"hashtags"`: 늘 붙일 해시태그
- 색을 바꾸려면 `"colors"`

## 6-1단계(선택). 법령 조회 이용자 ID 발급
법령 세트는 지금 공용 ID(`test`)로도 동작하지만, 공식으로는 내 ID를 쓰는 게 맞아요.
1. https://open.law.go.kr 회원가입 → **OPEN API 신청**(무료)
2. 발급된 **이용자 ID(이메일 앞부분)** 를 저장소 `data/settings.json` 의 `"law_oc": "내아이디"` 로 넣거나, 비밀 보관함에 `LAW_OC` 로 저장
3. 안 넣어도 카드는 만들어져요(공용 ID 사용).

## 7단계(선택). 토큰 자동 갱신
1. GitHub 오른쪽 위 프로필 → **Settings → Developer settings → Personal access tokens → Fine-grained tokens → Generate new token**
2. Repository access: **Only select repositories → cardnews**
3. Permissions → Repository permissions → **Secrets: Read and write**
4. 만든 토큰을 3단계 비밀 보관함에 이름 `GH_PAT` 으로 저장
   - 그러면 매일 아침 만들 때 20일마다 인스타 토큰을 새로 받아 바꿔 둬요. (이 토큰도 만료일이 있으니 넉넉히 1년으로)

## 8단계. 노트북 매일 실행 켜기 (완료됨)
- 윈도우 작업 스케줄러에 **CardnewsGovDaily** 가 등록돼 있어요(매일 07:00 + 로그인할 때).
- 기록은 `cardnews\logs\laptop.log` 에 남아요.
- 지금 바로 만들고 싶으면 `카드뉴스_지금만들기.bat` 더블클릭
- 다른 보도자료로 만들고 싶으면 `카드뉴스_보도자료로만들기.bat` 더블클릭 → 주소 붙여넣기
- 다시 등록해야 하면: PowerShell에서 `cardnews/scripts/register_task.ps1` 실행

## 9단계. 첫 실행
1. 노트북에서 `카드뉴스_지금만들기.bat` 실행(정부 보도자료 카드)
2. 뉴스 카드는 저장소 **Actions → 매일 뉴스 카드 만들기 → Run workflow**
3. 3~5분 뒤 확인 페이지 주소를 열면 오늘 카드가 보여요
4. 카드 아래 **[인스타에 올리기]** 안내대로 **모드 = 연습**으로 먼저 해 보세요
5. 잘 되면 다음부터는 매일 07:00쯤 자동으로 만들어지고, 실제로 올릴 때만 **모드 = 실제 + 확인 칸 `올립니다`**

---

### 자주 묻는 것
- **노트북이 꺼져 있어도 되나요?** 뉴스 카드·확인 페이지·인스타 올리기는 돼요. 정부 보도자료 카드만 노트북이 켜질 때 만들어져요.
- **돈이 드나요?** 공개 저장소는 GitHub Actions·Pages가 무료예요. 인스타·네이버 API도 무료예요.
- **60일 동안 아무것도 안 하면?** 매일 자동 기록이 쌓여서 괜찮아요. 혹시 Actions에 "disabled" 표시가 뜨면 **Enable workflow**를 눌러 주세요.
- **엉뚱한 보도자료가 골라졌어요.** 확인 페이지 아래 "오늘 살펴본 보도자료"에서 원하는 것의 주소를 복사 → 노트북에서 `카드뉴스_보도자료로만들기.bat` → 붙여 넣기.
- **GitHub 로그인이 풀렸대요(laptop.log).** `카드뉴스_지금만들기.bat`을 실행하면 로그인 창이 떠요. 직접 로그인해 주세요.
